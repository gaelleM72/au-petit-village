import { FilterByNamePipePipe } from './filter-by-name-pipe';

describe('FilterByNamePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByNamePipePipe();
    expect(pipe).toBeTruthy();
  });
});
